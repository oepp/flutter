name: oepp
description: https://github.com/oepp/flutter
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^3.2.0
  equatable: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^4.0.0
  mockito: ^4.1.1

flutter:
  uses-material-design: true